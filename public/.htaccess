Options -MultiViews
RewriteEngine On

# Redirigir a trailing slash para carpetas (rutas limpias)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_URI} !/$
RewriteRule ^(.*)$ /%{REQUEST_URI}/ [R=301,L]

# Página de error personalizada
ErrorDocument 404 /404/index.html
